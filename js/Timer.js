
function Timer(timer, second)
{
  this.timer = false;
  this.second = 0;
/*
  function timer(){
    if (!grid.timer) {
      grid.timer = true;
    }
    grid.second++;
    $('#timer').text(grid.second);

    compte=setTimeout(function() {
      if (!stopGame) {
        timer();
      }
    },1000)
  }
*/


}
